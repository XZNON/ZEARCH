// Prompt templates live here. Today there is one shared base prompt; Phase 2 adds
// per-archetype templates (person, event, place, …) that compose with the shared block.

export { SYSTEM_PROMPT } from './shared.js';
